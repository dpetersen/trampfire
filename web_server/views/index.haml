%a{ href: "/logout" } Log Out

%ul#tags
  - Tag.all.each do |tag|
    %li= tag.name

#transcript{ data: { email: "#{env['warden'].user.email}" } }
  %p Initial

%form
  %input#outgoing.xlarge{ type: 'textarea' }
  %input.btn.primary{ type: 'submit' }
