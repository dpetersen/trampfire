#left-main
  #transcript-wrapper.well
    #transcript{ data: { email: "#{env['warden'].user.email}" } }
      - @messages.each do |message|
        %dl
          %dt #{message.user.display_name} @ #{message.tag.name}
          %dd= message.final_message

  #chat
    %form
      .control-wrapper
        %input#outgoing.xlarge.disabled{ type: 'textarea' }
        %input.btn.success.disabled{ type: 'submit' }
    %strong Active Tag:
    %em.activeTag loading...

#sidebar
  %form#new-tag.well{ action: "/tags", method: "post" }
    %input.small{ type: 'text', name: 'tag[name]' }
    %input.btn.primary{ type: 'submit', value: "Create Tag" }

  #tags.well
    %h3 Tags
    %ul#tags.pills
      - Tag.all.each do |tag|
        %li
          %a{ href: "#" }= tag.name

  #roster.well
    %h3 Roster
    %ul.unstyled
